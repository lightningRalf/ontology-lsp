#!/usr/bin/env bash
set -euo pipefail

HERE="$(cd "$(dirname "$0")" && pwd)"

# Prefer local repo build
if command -v bun >/dev/null 2>&1; then
  exec bun "$HERE/dist/cli/cli.js" "$@"
fi

# Fallback to Node if bun not available
exec node "$HERE/dist/cli/cli.js" "$@"
