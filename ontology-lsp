#!/usr/bin/env bash
set -euo pipefail

# Prefer local repo build
if command -v bun >/dev/null 2>&1; then
  exec bun dist/cli/cli.js "$@"
fi

# Fallback to Node if bun not available (requires bun-shebang compatible file)
exec node dist/cli/cli.js "$@"
